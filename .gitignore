node_modules/
dist/
*.log
*.tsbuildinfo

# build outputs inside the plugin bundle
packages/plugin/com.vitamin.speaker-for-discord.sdPlugin/bin/
packages/plugin/com.vitamin.speaker-for-discord.sdPlugin/logs/

# packaged plugin
*.streamDeckPlugin

# local secrets must never land here, but belt-and-braces
.env
.env.*
auth.json
session.key

.claude/
logs/

# NOTE: docs/ is intentionally NOT ignored — INSTALL.md / DEVELOPMENT.md / PRIVACY.md
# are part of the published repo and are linked from README.md.
